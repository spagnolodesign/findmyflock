<div class="container">
  <div class="row justify-content-md-center">
    <div class="col col-md-8">
      <%= render 'utils/progressbar', step: 2, navigations: ["Job profile", "Skills", "Benefits and Cultures"], classname:"" %>
      <div class="card">
        <div class="card-body">
          <%= render 'utils/skills_form', resource: @job, url_api: "jobs" %>
          <div class="text-center">
            <%= link_to "Continue", benefits_job_path, class: "btn btn-primary btn-lg mt-4 mb-2" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
