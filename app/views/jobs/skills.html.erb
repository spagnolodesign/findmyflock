<div class="container">
  <div class="row justify-content-md-center">
    <div class="col col-md-8">
      <%= render 'utils/progressbar', step: 2, navigations: ["Job profile", "Skills", "Benefits and Cultures"] %>
      <div class="card">
        <div class="card-body">
          <%= render 'utils/skills_form', resource: @job, url_api: "jobs" %>
          <%= link_to "Next", benefits_job_path, class: "btn btn-primary" %>
        </div>
      </div>
    </div>
  </div>
</div>
