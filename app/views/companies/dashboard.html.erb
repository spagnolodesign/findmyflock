<div class="navbar navbar-expand-md navbar-light fixed-top" id="NavContent" style="background-color: white;">
  <a href="/">
    <img class="navbar-brand ml-auto" id="Logo" src="/assets/flock_logo.png" alt="Flock logo">
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse justify-content-end pr-4" id="navbarCollapse">
    <ul class="navbar-nav">
      <li class="nav-item">
        <div class="avatar-navbar p-3">
        </div>
      </li>
      <li>
        <div class="dropdown">
          <button class="btn dropdown-navbar dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          </button>
          <div style="font-family= Roboto;" class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
            <a class="pl-2"><%= link_to "Edit your profile", edit_profile_company_path(@company) %></a>
            <a class="pl-2"><%= link_to "Edit your skills", add_skills_company_path(@company) %></a>
            <a class="pl-2"><%= link_to "Account settings", edit_company_registration_path(@company) %></a>
          </div>
        </div>
      </li>
    </ul>
  </div>
</div>



<h3>Dashboard for company <%= @company.name %></h3>

<% if recruiter_signed_in? %>
  <%= link_to('Logout Recruiter', destroy_recruiter_session_path, method: :delete) %> -
  <%= link_to "Post a job", new_job_path %>
<% end %>


<div class="row">
  <div class="col-4">
    <%= render "utils/carousel", images: @company.images %>
  </div>
</div>


<div class="row">
  <div class="col-4">
    <b>Active jobs:</b>
    <br />
    <% @jobs.each_with_index do |job, index| %>
      <%= index + 1 %> - <%= job.title %> <%= link_to "Edit", edit_job_path(job) %><br/>
    <% end %>
  </div>
</div>

<br />

<div class="row">
  <div class="col-4">
    <b>Your job applications:</b>
    <br />
    <% @company.applications.each_with_index do |application, index| %>
      <p><%= index+1 %> For: <%= application.match.job.title %> - State: <%= application.status %> <%= link_to "See application", job_application_path(application.match.job, application) %></p>
    <% end %>
  </div>
</div>
